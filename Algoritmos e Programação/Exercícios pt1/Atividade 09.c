#include <stdio.h>
int main () {
    float base, altura, area, total;

    printf ("Digite o valor da base:\n");
    scanf ("%f*%c", &base);

    printf ("Digite o valor da altura:\n");
    scanf ("%f*%c", &altura);

    area= (base * altura);
    total = (area/2);

    printf ("A area da figura é: %.2f/n", total);












}
